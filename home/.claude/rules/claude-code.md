# Claude Code

- ユーザーに質問するときは、積極的に `AskUserQuestion` ツールを使います
- パイプの後ろで標準入力をフィルタするときは、`rg -uuu <pattern> -` のように末尾に `-` を付けて標準入力を明示します（パス無しだと権限チェッカーにカレントディレクトリ全体の検索と判定され、ファイル読み取りの権限ルールに触れて確認プロンプトが出ることがあるため）
